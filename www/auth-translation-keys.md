# Clés de traduction pour le module AUTH

Ce document liste toutes les clés de traduction identifiées dans le module AUTH.

## AUTH.LOGIN - Page de connexion

### En-tête
- `AUTH.LOGIN.TITLE` → "Welcome to Globrel"
- `AUTH.LOGIN.SUBTITLE` → "Sign in to continue to Globrel."

### Formulaire
- `AUTH.LOGIN.FORM.USERNAME_LABEL` → "Username"
- `AUTH.LOGIN.FORM.USERNAME_PLACEHOLDER` → "Enter username"
- `AUTH.LOGIN.FORM.PASSWORD_LABEL` → "Password"
- `AUTH.LOGIN.FORM.PASSWORD_PLACEHOLDER` → "Enter password"
- `AUTH.LOGIN.FORM.REMEMBER_ME` → "Remember me"
- `AUTH.LOGIN.FORM.FORGOT_PASSWORD` → "Forgot password?"
- `AUTH.LOGIN.FORM.LOGIN_BUTTON` → "Log In"
- `AUTH.LOGIN.FORM.LOGGING_IN` → "Logging in..."

### Messages
- `AUTH.LOGIN.ERROR.INVALID_CREDENTIALS` → "Invalid email or password."

### Liens
- `AUTH.LOGIN.NO_ACCOUNT` → "Don't have an account?"
- `AUTH.LOGIN.FREE_REGISTER` → "Free Register"
- `AUTH.LOGIN.OR_LOGIN_WITH` → "Or Login With"

---

## AUTH.REGISTER - Page d'inscription

### En-tête
- `AUTH.REGISTER.TITLE` → "Create an account"
- `AUTH.REGISTER.SUBTITLE` → "Enter your details to create your account today."

### Formulaire
- `AUTH.REGISTER.FORM.FIRSTNAME_LABEL` → "First Name"
- `AUTH.REGISTER.FORM.FIRSTNAME_PLACEHOLDER` → "Enter first name"
- `AUTH.REGISTER.FORM.FIRSTNAME_ERROR` → "Please enter your first name"
- `AUTH.REGISTER.FORM.LASTNAME_LABEL` → "Last Name"
- `AUTH.REGISTER.FORM.LASTNAME_PLACEHOLDER` → "Enter last name"
- `AUTH.REGISTER.FORM.LASTNAME_ERROR` → "Please enter your last name"
- `AUTH.REGISTER.FORM.EMAIL_LABEL` → "Email"
- `AUTH.REGISTER.FORM.EMAIL_PLACEHOLDER` → "Enter email"
- `AUTH.REGISTER.FORM.EMAIL_ERROR` → "Please enter a valid email"
- `AUTH.REGISTER.FORM.PASSWORD_LABEL` → "Password"
- `AUTH.REGISTER.FORM.PASSWORD_PLACEHOLDER` → "Enter password"
- `AUTH.REGISTER.FORM.PASSWORD_ERROR` → "Please enter your password"
- `AUTH.REGISTER.FORM.CONFIRM_PASSWORD_LABEL` → "Confirm Password"
- `AUTH.REGISTER.FORM.CONFIRM_PASSWORD_PLACEHOLDER` → "Confirm password"
- `AUTH.REGISTER.FORM.CONFIRM_PASSWORD_ERROR` → "New password and confirm password don't match"
- `AUTH.REGISTER.FORM.MOBILE_LABEL` → "Mobile Number"
- `AUTH.REGISTER.FORM.MOBILE_PLACEHOLDER` → "Enter mobile number"
- `AUTH.REGISTER.FORM.MOBILE_ERROR` → "Please enter a valid mobile number"
- `AUTH.REGISTER.FORM.TERMS_TEXT` → "By registering you agree to the Rizz"
- `AUTH.REGISTER.FORM.TERMS_LINK` → "Terms of Use"
- `AUTH.REGISTER.FORM.REGISTER_BUTTON` → "Register"
- `AUTH.REGISTER.FORM.REGISTERING` → "Registering..."

### Liens
- `AUTH.REGISTER.HAVE_ACCOUNT` → "Already have an account?"
- `AUTH.REGISTER.LOGIN_LINK` → "Log in"

---

## AUTH.RECOVER_PW - Récupération de mot de passe

### En-tête
- `AUTH.RECOVER_PW.TITLE` → "Reset Password"
- `AUTH.RECOVER_PW.SUBTITLE` → "Enter your Email and instructions will be sent to you!"

### Formulaire
- `AUTH.RECOVER_PW.FORM.EMAIL_LABEL` → "Email"
- `AUTH.RECOVER_PW.FORM.EMAIL_PLACEHOLDER` → "Enter Email Address"
- `AUTH.RECOVER_PW.FORM.RESET_BUTTON` → "Reset"
- `AUTH.RECOVER_PW.FORM.SENDING` → "Envoi en cours..."

### Messages
- `AUTH.RECOVER_PW.ERROR.INVALID_EMAIL` → "Veuillez entrer une adresse email valide."
- `AUTH.RECOVER_PW.SUCCESS.EMAIL_SENT` → "Un email de récupération a été envoyé à votre adresse."

### Liens
- `AUTH.RECOVER_PW.REMEMBER_IT` → "Remember It ?"
- `AUTH.RECOVER_PW.SIGNIN_LINK` → "Sign in here"

---

## AUTH.NEW_PW - Nouveau mot de passe

### En-tête
- `AUTH.NEW_PW.TITLE` → "Reset Password"
- `AUTH.NEW_PW.SUBTITLE` → "Enter your Email and instructions will be sent to you!"

### Formulaire
- `AUTH.NEW_PW.FORM.CODE_LABEL` → "Code"
- `AUTH.NEW_PW.FORM.CODE_PLACEHOLDER` → "Enter code you get on email"
- `AUTH.NEW_PW.FORM.PASSWORD_LABEL` → "Password"
- `AUTH.NEW_PW.FORM.PASSWORD_PLACEHOLDER` → "Enter password"
- `AUTH.NEW_PW.FORM.CONFIRM_PASSWORD_LABEL` → "Confirm password"
- `AUTH.NEW_PW.FORM.CONFIRM_PASSWORD_PLACEHOLDER` → "Enter password again"
- `AUTH.NEW_PW.FORM.RESET_BUTTON` → "Reset"
- `AUTH.NEW_PW.FORM.SENDING` → "Envoi en cours..."

### Messages
- `AUTH.NEW_PW.ERROR.VALIDATION` → "Veuillez remplir tous les champs et vérifier que les mots de passe sont identiques."
- `AUTH.NEW_PW.SUCCESS.PASSWORD_RESET` → "Votre mot de passe a été réinitialisé avec succès."

### Liens
- `AUTH.NEW_PW.REMEMBER_IT` → "Remember It ?"
- `AUTH.NEW_PW.SIGNIN_LINK` → "Sign in here"

---

## AUTH.VERIFY - Vérification email

### En-tête
- `AUTH.VERIFY.TITLE` → "Bravo, vous avez reçu un code de vérification ! verifiez vos mails"
- `AUTH.VERIFY.SUBTITLE` → "Il se peut aussi qu'il soit dans vos spam."

### Formulaire
- `AUTH.VERIFY.FORM.EMAIL_LABEL` → "email"
- `AUTH.VERIFY.FORM.CODE_LABEL` → "Code"
- `AUTH.VERIFY.FORM.CODE_PLACEHOLDER` → "Entrez le code de vérification"
- `AUTH.VERIFY.FORM.VERIFY_BUTTON` → "Verifier"
- `AUTH.VERIFY.FORM.VERIFYING` → "Logging in..."

### Messages
- `AUTH.VERIFY.ERROR.INVALID` → "Invalid email or password."
- `AUTH.VERIFY.SUCCESS.EMAIL_RESENT` → "Un email a été renvoyé à votre adresse. Veuillez vérifier votre boîte de réception."

### Liens
- `AUTH.VERIFY.NO_CODE` → "Vous n'avez pas recu de code ?"
- `AUTH.VERIFY.RESEND_LINK` → "Me renvoyer un nouveau code"

---

## AUTH.ERROR404 - Page erreur 404

- `AUTH.ERROR404.TITLE` → "Oops! Sorry page does not found"
- `AUTH.ERROR404.SUBTITLE` → "Back to dashboard of Globrel"
- `AUTH.ERROR404.ERROR_CODE` → "404!"
- `AUTH.ERROR404.ERROR_MESSAGE` → "Somthing went wrong"
- `AUTH.ERROR404.BACK_BUTTON` → "Back to Dashboard"

---

## AUTH.ERROR500 - Page erreur 500

- `AUTH.ERROR500.TITLE` → "Sorry! Unexpected Server Error"
- `AUTH.ERROR500.SUBTITLE` → "Back to dashboard of Rizz"
- `AUTH.ERROR500.ERROR_CODE` → "500!"
- `AUTH.ERROR500.ERROR_MESSAGE` → "Internal Server Error"
- `AUTH.ERROR500.BACK_BUTTON` → "Back to Dashboard"

---

## AUTH.MAINTENANCE - Page de maintenance

- `AUTH.MAINTENANCE.TITLE` → "We are down on maintenance right now on v {{appVersion}}."
- `AUTH.MAINTENANCE.SUBTITLE` → "This website is currently undergoing Scheduled maintenance."
- `AUTH.MAINTENANCE.ADMIN_BUTTON` → "Si tu es admin,connect toi"
- `AUTH.MAINTENANCE.HOME_BUTTON` → "Si tu veux voir l'acceuil"

---

## Total des clés: ~100 clés pour le module AUTH
